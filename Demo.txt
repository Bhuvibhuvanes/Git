
java is an object oriented language

git restore filename.extension

git restore --staged Demo.txt

git commit -am "one more command added"

git config --global alias.hist "log --pretty=format:'%h %ad | %s%d[%an]' --graph --date=short"

git show head

git tag Demotag

git tag -d Demotag

git commnd

git tag 

git help log 

git show v1.0

git switch branchname

git checkout branchname

git mv oldfilename.extension newfilename.extension

git restore newfilename.extension

git diff hascode head

git branch -b newbranchname

git commit --amend -m "git new message"

git show v1.0


Automatic mearch :
main branch -sub branch :sub branch all commit added to main branch

Fast forward:
two file have ,one file commit in sub branch and another file commit in main brnch is called fast forward

merge conflict:
error creation how to rectify it

git branch -D Learning (force to delete a branch)



